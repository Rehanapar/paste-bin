/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // optional, recommended
  swcMinify: true,       // optional, for faster builds
};

module.exports = nextConfig;


